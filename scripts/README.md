# scripts

Helper scripts used for validation (run in CI) and demos.

| Script | Purpose |
| --- | --- |
| `check-rules.sh` | Syntactic sanity check for committed Snort `.rules` files |

Run everything via the Makefile: `make validate`.
